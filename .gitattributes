# git-crypt: alles verschlüsseln außer explizit ausgenommene Dateien
* filter=git-crypt diff=git-crypt

# Klartext-Ausnahmen (git-crypt-Metadaten + diese Steuerdateien)
.gitattributes !filter !diff
.gitignore !filter !diff
.git-crypt/** !filter !diff

# Öffentliche Nutzer-Doku (bewusst KLARTEXT, damit sie auf der Codeberg-Seite rendert —
# enthält KEINE Geheimnisse, nur Install-Anleitung + Signatur-Fingerprint)
INSTALL.md !filter !diff
