# Exportierte Vault-Dateien gehören NICHT ins Repo (enthalten verschlüsselte Bestände)
*.vault

# Echte Bestands-/Export-Daten — NIE ins (öffentliche!) Repo.
# Die App versendet keine CSV als Repo-Asset; alle .csv sind Nutzerdaten.
*.csv

# lokale Secrets / Daten
.env
.env.*
data/

# OS-Müll
.DS_Store
Thumbs.db

# lokale Test-Helfer (maschinenspezifischer playwright-core/Brave-Pfad)
serve-test.mjs
ui-roundtrip-test.mjs
csv-import-test.mjs
verify-*.mjs
verify-*.png

# Capacitor / Android-Build (regenerierbar aus web-Assets + capacitor.config.json)
node_modules/
www/
android/
gradle-build.log
release-build.log

# SIGNATUR-GEHEIMNISSE — NIEMALS committen
*.keystore
*.jks
keystore.properties
release-signing.properties
