app-id: org.alieninvestor.tresor
runtime: org.freedesktop.Platform
runtime-version: '25.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '25.08'
command: sachwert-tresor
separate-locales: false
# Bewusst KEIN --share=network und KEIN --filesystem: Dateien nur über das Portal (Dialog), Netz gibt es im Käfig nicht.
finish-args:
  - --socket=wayland
  - --socket=fallback-x11
  - --share=ipc
  - --device=dri
modules:
  - name: sachwert-tresor
    buildsystem: simple
    build-options:   # Electron-Binaries unverändert lassen (gepinnter Hash + Fuses), kein Debug-Ableger
      no-debuginfo: true
      strip: false
    sources:
      - type: dir
        path: ../build/electron
        dest: electron
      - type: file
        path: sachwert-tresor.sh
      - type: file
        path: org.alieninvestor.tresor.desktop
      - type: file
        path: ../../icon.svg
    build-commands:
      - mkdir -p /app/main && cp -a electron/. /app/main/
      - install -Dm755 sachwert-tresor.sh /app/bin/sachwert-tresor
      - install -Dm644 org.alieninvestor.tresor.desktop /app/share/applications/org.alieninvestor.tresor.desktop
      - install -Dm644 icon.svg /app/share/icons/hicolor/scalable/apps/org.alieninvestor.tresor.svg
